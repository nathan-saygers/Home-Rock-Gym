// Dependencies
import React from "react";
import styles from "./HoldSearchBar.module.scss";

const HoldSearchBar = (props) => {
  return <div className={styles.searchBarContainer}>I'm a search bar!</div>;
};

export default HoldSearchBar;
